rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users can read
    // All writes go through the backend (Admin SDK bypasses rules)
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
